{
  "name": "api-server",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm --filter @workspace/ws build && tsx watch src/server.ts",
    "start": "node dist/server.js",
    "build": "pnpm --filter @workspace/ws build && tsc -p tsconfig.json"
  },
  "dependencies": {
    "@workspace/shared": "workspace:*",
    "@workspace/ws": "workspace:*",
    "cors": "^2.8.6",
    "express": "^5.2.1"
  },
  "devDependencies": {
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.6",
    "@types/node": "^25.2.2",
    "@workspace/typescript-config": "workspace:*",
    "ts-node": "^10.9.2",
    "tsx": "^4.20.6",
    "typescript": "^5.9.3"
  }
}
