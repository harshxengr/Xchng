{
  "name": "xchng",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "dotenv -- turbo run dev",
    "dev:infra": "docker compose up -d",
    "dev:db": "dotenv -- turbo run db:generate db:push",
    "dev:start": "pnpm dev:infra && pnpm dev:db",
    "dev:stop": "docker compose down; pkill -f '[t]sx.*watch' || true; pkill -f '[n]ext dev' || true; pkill -f '[t]urbo run dev' || true",
    "build": "dotenv -- turbo run build",
    "build:web": "dotenv -- turbo run build --filter=web...",
    "build:backend": "dotenv -- turbo run build --filter=api-server...",
    "start": "dotenv -- turbo run start",
    "start:backend": "dotenv -- pnpm --filter @workspace/database db:deploy && pnpm --filter api-server start",
    "db:generate": "dotenv -- pnpm --filter @workspace/database db:generate",
    "db:push": "dotenv -- pnpm --filter @workspace/database db:push",
    "db:deploy": "dotenv -- pnpm --filter @workspace/database db:deploy",
    "lint": "turbo run lint",
    "check-types": "dotenv -- turbo run check-types",
    "test": "dotenv -- turbo run test",
    "ci": "pnpm lint && pnpm check-types && pnpm build"
  },
  "devDependencies": {
    "dotenv-cli": "^8.0.0",
    "turbo": "^2.3.3"
  },
  "packageManager": "pnpm@10.33.2"
}
