{
  "name": "xchng",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "turbo run dev",
    "dev:infra": "docker-compose up -d",
    "dev:db": "pnpm --filter @workspace/database db:push && pnpm --filter @workspace/database db:generate && pnpm --filter @workspace/database build",
    "dev:start": "pnpm dev:infra && pnpm dev:db",
    "dev:stop": "docker-compose down && pkill -f 'tsx watch' && pkill -f 'next dev'",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "check-types": "turbo run check-types"
  },
  "devDependencies": {
    "turbo": "^2.4.4"
  },
  "packageManager": "pnpm@10.25.0"
}
